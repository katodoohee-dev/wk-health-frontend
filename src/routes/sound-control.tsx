import { createFileRoute } from "@tanstack/react-router";
import { Lightbulb, ArrowUpRight, Map, Navigation } from "lucide-react";
import { AppShell } from "@/components/wk/shell";
export const Route=createFileRoute("/sound-control")({component:InsightOrRoute});
function InsightOrRoute(){const route=typeof window!=="undefined"&&window.location.pathname==="/route";return <AppShell title={route?"Route":"Insight"}>{route?<RoutePage/>:<InsightPage/>}</AppShell>}
function InsightPage(){return <section className="mx-auto max-w-4xl"><p className="label-xs">WK intelligence</p><h1 className="display mt-2 text-4xl">Small signals. Useful changes.</h1><div className="mt-8 space-y-3">{[["Training days","Dinner is later on strength days.","Bring dinner forward by 40 minutes."],["Sleep","Your sleep timing is becoming more regular.","Keep the current evening routine."],["Movement","Weekly movement is trending upward.","Maintain the current load."]].map(([a,b,c])=><article key={a} className="grid gap-5 border border-border bg-card p-6 sm:grid-cols-[auto_1fr_auto] sm:items-center"><Lightbulb className="size-5"/><div><p className="label-xs">{a}</p><h2 className="mt-2 font-medium">{b}</h2><p className="mt-1 text-sm text-muted-foreground">{c}</p></div><ArrowUpRight className="size-4 text-muted-foreground"/></article>)}</div></section>}
function RoutePage(){return <section className="mx-auto max-w-5xl"><p className="label-xs">Movement map</p><h1 className="display mt-2 text-4xl">Route</h1><div className="mt-8 grid min-h-96 place-items-center border border-border bg-card"><div className="text-center"><Map className="mx-auto size-8"/><p className="mt-4 font-medium">Route history</p><p className="mt-2 text-sm text-muted-foreground">Your movement paths will appear here.</p><Navigation className="mx-auto mt-6 size-4 text-signal"/></div></div></section>}
