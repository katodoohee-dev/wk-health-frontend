import { createFileRoute } from '@tanstack/react-router';
import { AppShell } from '@/components/wk/app-shell';
import { SectionHeader } from '@/components/wk/primitives';
export const Route=createFileRoute('/settings')({component:SettingsPage});
type Row=[string,string,string]; type Group=[string,string,Row[]];
const groups:Group[]=[['Measurement','Units & baselines',[['Distance','Used across activity and navigation','Kilometres'],['Weight','Scale sync and body trends','Kilograms'],['Week starts','Affects weekly rollups','Monday'],['Baseline window','Rolling comparison period','30 days']]],['Assistant','Voice & coaching',[['Wake phrase','"Hey WK" on band and phone','On'],['Voice character','Neutral, measured delivery','Calm'],['Coaching tone','How direct guidance should be','Direct'],['Duck music on cues','Lower playback while speaking','On']]],['Privacy','Data & sharing',[['On-device processing','Keep raw signals local where possible','On'],['Share with friends','Only streaks and active minutes','Limited'],['Research contribution','Anonymised aggregate only','Off']]],['Accessibility','Display & motion',[['Reduce motion','Follows your system preference','System'],['Larger numerals','Increases metric type scale','Off'],['High contrast borders','Strengthens hairlines','On']]]];
function SettingsPage(){return <AppShell eyebrow="Configuration" title="Settings"><div className="grid gap-14 lg:grid-cols-[1.6fr_1fr]">{groups.map(([eyebrow,title,rows])=><section key={eyebrow}><SectionHeader eyebrow={eyebrow} title={title}/><div className="divide-y divide-border">{rows.map(([label,hint,value])=><div key={label} className="grid grid-cols-[1fr_auto] gap-4 py-5"><div><b className="text-sm">{label}</b><p className="text-xs text-muted-foreground">{hint}</p></div><span className="text-xs text-muted-foreground">{value}</span></div>)}</div></section>)}</div></AppShell>}
