import { createFileRoute } from '@tanstack/react-router';
import { AppShell } from '@/components/wk/app-shell';
import { Eyebrow, Panel, SectionHeader } from '@/components/wk/primitives';
export const Route=createFileRoute('/settings')({component:SettingsPage});
const groups=[['Measurement','Units & baselines',[['Distance','Used across activity and navigation','Kilometres'],['Weight','Scale sync and body trends','Kilograms'],['Week starts','Affects weekly rollups','Monday'],['Baseline window','Rolling comparison period','30 days']]],['Assistant','Voice & coaching',[['Wake phrase','"Hey WK" on band and phone','On'],['Voice character','Neutral, measured delivery','Calm'],['Coaching tone','How direct guidance should be','Direct'],['Duck music on cues','Lower playback while speaking','On']]],['Privacy','Data & sharing',[['On-device processing','Keep raw signals local where possible','On'],['Share with friends','Only streaks and active minutes','Limited'],['Research contribution','Anonymised aggregate only','Off']]],['Accessibility','Display & motion',[['Reduce motion','Follows your system preference','System'],['Larger numerals','Increases metric type scale','Off'],['High contrast borders','Strengthens hairlines','On']]]];
function SettingsPage(){return <AppShell eyebrow="Configuration" title="Settings"><div className="grid gap-14 lg:grid-cols-[1.6fr_1fr]">{groups.map(g=><section key={g[0]}><SectionHeader eyebrow={g[0] as string} title={g[1] as string}/><div className="divide-y divide-border">{(g[2] as string[][]).map(r=><div key={r[0]} className="grid grid-cols-[1fr_auto] gap-4 py-5"><div><b className="text-sm">{r[0]}</b><p className="text-xs text-muted-foreground">{r[1]}</p></div><span className="text-xs text-muted-foreground">{r[2]}</span></div>)}</div></section>)}</div></AppShell>}
