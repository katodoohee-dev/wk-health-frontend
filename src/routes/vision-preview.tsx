import { createFileRoute } from '@tanstack/react-router';
import { MobileVision } from '@/components/wk-mobile-variants';
export const Route=createFileRoute('/vision-preview')({component:MobileVision});
