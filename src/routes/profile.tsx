import { createFileRoute } from '@tanstack/react-router';
import { AppShell } from '@/components/wk/app-shell';
import { Chip, Eyebrow, Panel, SectionHeader, Sparkline } from '@/components/wk/primitives';
import { user } from '@/lib/wk-data';
export const Route=createFileRoute('/profile')({component:ProfilePage});
const baselines=[['VO₂ max','54.2','ml/kg/min'],['Resting HR baseline','56','bpm'],['HRV baseline','61','ms'],['Threshold pace','4:22','/km']];
function ProfilePage(){return <AppShell eyebrow="Account" title="Profile"><section className="panel grain grid gap-8 p-6 sm:p-8 lg:grid-cols-[1fr_auto] lg:items-end"><div className="flex items-center gap-5"><span className="numeric grid size-20 place-items-center rounded-full border border-border text-xl">{user.initials}</span><div><Eyebrow>{user.plan}</Eyebrow><h2 className="display mt-1 text-4xl">{user.name}</h2><p className="mt-1 text-sm text-muted-foreground">{user.handle} · with WK since {user.since}</p></div></div><div className="flex gap-2"><Chip tone="signal">Band connected</Chip><Chip>On-device processing</Chip></div></section><section className="mt-14"><SectionHeader eyebrow="Baselines" title="Your physiology"/><div className="mt-8 grid gap-6 sm:grid-cols-2 xl:grid-cols-4">{baselines.map(b=><Panel key={b[0]}><Eyebrow>{b[0]}</Eyebrow><p className="numeric mt-4 text-4xl">{b[1]}</p><p className="eyebrow mt-1">{b[2]}</p></Panel>)}</div></section><section className="mt-14 grid gap-6 lg:grid-cols-2"><Panel><Eyebrow>Twelve-month trajectory</Eyebrow><p className="display mt-2 text-2xl">Readiness is compounding</p><Sparkline data={[62,60,66,64,69,72,70,75,78,80,84,87]} height={70} className="mt-8 h-24"/></Panel><Panel><SectionHeader eyebrow="Hardware" title="Devices"/><div className="divide-y divide-border">{[['WK Band 2','Battery 78% · synced 2m ago','Live'],['Chest strap H10','Last used yesterday','Idle'],['Smart scale','Weekly weigh-in Sunday','Idle']].map(x=><div key={x[0]} className="flex justify-between gap-4 py-4"><div><b className="text-sm">{x[0]}</b><p className="text-xs text-muted-foreground">{x[1]}</p></div><Chip tone={x[2]==='Live'?'signal':'default'}>{x[2]}</Chip></div>)}</div></Panel></section></AppShell>}
