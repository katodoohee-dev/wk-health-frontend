services:
  - type: web
    name: wk-health-frontend
    runtime: node
    plan: free
    branch: main
    buildCommand: npm install --no-audit --no-fund && npm run build
    startCommand: node .output/server/index.mjs
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 22.12.0
